# syntax=docker/dockerfile:1

# Development Dockerfile for Nuxt frontend with hot reload
FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Expose port
EXPOSE 3000

# Run development server with hot reload
CMD ["npm", "run", "dev"]
